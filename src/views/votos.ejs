<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=titulo%>
    </title>
</head>

<body>
    <header>
        <h1>
            <%=titulo%>
        </h1>
    </header>
    <main>
        <section>
            <h2>Votos</h2>

            <% if (votos && voto.length> 0) { %>
                <ul>
                    <% voto.forEach(candidato=> { %>
                        <li>
                            <a href="/voto/<%= voto.candidatoId %>">
                                <%= candidato.numeroUrna %>
                            </a>
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <p>Nenhum candidato encontrado.</p>
                    <% } %>
        </section>
    </main>

</body>

</html>