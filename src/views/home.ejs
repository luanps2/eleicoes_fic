
    <main>
        <section>
            <h2>teste do servidor</h2>

            <% if (candidatos && candidatos.length> 0) { %>
                <ul>
                    <% candidatos.forEach(candidato=> { %>
                        <li>
                            <a href="/candidato/<%= candidato.numeroUrna %>">
                                <%= candidato.numeroUrna %>
                            </a>
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <p>Nenhum candidato encontrado.</p>
                    <% } %>
        </section>
    </main>

